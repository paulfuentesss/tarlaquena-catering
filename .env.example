# Neon connection string, e.g. postgres://user:password@ep-xxxx.neon.tech/dbname?sslmode=require
DATABASE_URL=

# Clerk (https://dashboard.clerk.com) — Configure API Keys page
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Email allowed to access /admin (must match the signed-in Clerk user's email exactly)
ADMIN_EMAIL=
